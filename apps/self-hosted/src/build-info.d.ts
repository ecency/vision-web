/** Baked by rsbuild's source.define from GIT_SHA at build time. */
declare const __BUILD_SHA__: string;
/** The canonical release version (self-hosted-vX.Y.Z tag); '' when untagged. */
declare const __BUILD_VERSION__: string;
