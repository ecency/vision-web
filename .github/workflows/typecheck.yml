name: Typecheck
# Mirrors lint.yml: pull_request (not a branch-filtered push) so a required check still
# reports on PRs whose head branch doesn't match a naming convention — Dependabot/Renovate/
# hotfix/bot branches — which would otherwise be permanently blocked once this is required.
on:
  pull_request:
# Typecheck only — no writes back to GitHub.
# Superseded runs are cancelled: pushing again to a PR makes the previous result irrelevant,
# so letting it finish only burns runner minutes.
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  typecheck:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [24.x]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with:
          version: 11.5.0
          run_install: false
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      # Each package typechecks its own source; the apps resolve packages through the
      # committed dist/*.d.ts, which the release bot keeps in step on version bumps.
      - name: Typecheck
        run: pnpm -r --if-present typecheck
      # Icon sizing convention (docs/icons.md). Both layers are enforcing:
      # the SCSS ledger fails on unowned or resurrected rules, and the TSX
      # audit fails on any finding (lock bundle flipped it to --fail).
      - name: Icon convention audit (SCSS ledger)
        run: node scripts/icon-scss-audit.mjs
      - name: Icon convention audit (TSX)
        run: node scripts/icon-tsx-audit.mjs --fail
      # Slim-entry convention (apps/web/src/core/entries/slim-entry.ts). Enforces
      # how a feed query is slimmed: a single-page builder needs its own cache
      # identity, because deck columns read those page keys and render the body
      # from them, and an infinite builder must not take one, because the feed
      # poll hand-builds that key for its merge.
      - name: Slim-entry convention audit
        run: node scripts/slim-entries-audit.mjs --fail

      # This repo is PUBLIC and infra/origin holds production vhosts, so the audit keeps
      # thresholds, source addresses, inline allowlists and secret markers out of them, and
      # rejects a wildcard include on a file carrying a protective directive (nginx accepts
      # one matching no files and reloads clean, i.e. it fails OPEN). --self-test runs first
      # so a rule edited into uselessness fails here rather than passing silently.
      - name: Audit committed origin nginx config
        run: |
          node scripts/origin-config-audit.mjs --self-test
          node scripts/origin-config-audit.mjs --fail
